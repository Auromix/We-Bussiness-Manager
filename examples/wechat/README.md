# 企业微信机器人示例

本目录包含企业微信机器人的使用示例。

## 示例列表

### 1. basic_usage.py - 基础使用示例

演示如何：
- 初始化机器人
- 获取群聊列表
- 获取群聊信息和成员
- 发送消息
- 保持机器人运行

**运行：**
```bash
python examples/wechat/basic_usage.py
```

### 2. advanced_usage.py - 高级功能示例

演示如何：
- 使用群聊管理器（搜索、统计、批量操作）
- 使用用户管理器（查询用户、搜索、部门管理）
- 发送不同类型的消息（文本、Markdown、图片、文件）
- 配置回调服务器

**运行：**
```bash
python examples/wechat/advanced_usage.py
```

## 前提条件

1. 已配置环境变量（`.env` 文件）
2. 已安装所有依赖：
   ```bash
   pip install -r requirements.txt
   ```
3. 拥有企业微信应用的必要权限

## 配置说明

参考以下文档：
- [配置指南](../../interface/wechat/SETUP_GUIDE.md) - 详细的配置步骤
- [功能文档](../../interface/wechat/README.md) - 完整的功能说明

## 注意事项

1. **测试环境**
   - 建议先在测试群中进行测试
   - 不要在生产环境直接运行未测试的代码

2. **API 限制**
   - 注意企业微信的 API 调用频率限制
   - 批量操作时注意添加延迟

3. **权限配置**
   - 确保应用有相应的权限
   - 部分功能需要管理员权限

4. **敏感信息**
   - 示例代码中的 `chatid`、`userid` 等需要替换为实际值
   - 不要将真实的配置信息提交到代码库

## 快速测试

### 测试连接

```python
from config.settings import settings
from interface.wechat.wecom_client import WeChatWorkClient

client = WeChatWorkClient(
    corp_id=settings.wechat_work_corp_id,
    secret=settings.wechat_work_secret,
    agent_id=settings.wechat_work_agent_id
)

# 测试获取 token
token = client._get_access_token()
print(f"Access token: {token[:20]}...")

# 测试获取群聊列表
groups = client.get_all_app_chats()
print(f"找到 {len(groups)} 个群聊")
```

### 测试发送消息

```python
# 替换为实际的 chatid
chat_id = "your_chat_id"

# 发送测试消息
client.send_group_message(chat_id, "🤖 测试消息")
print("消息已发送")
```

## 常见问题

### Q: 为什么获取不到群聊列表？

A: 可能的原因：
1. 应用没有群聊管理权限
2. 还没有创建任何应用群聊
3. access_token 无效

### Q: 为什么发送消息失败？

A: 检查：
1. chatid 是否正确
2. 应用是否有发送消息权限
3. 群聊是否还存在

### Q: 如何创建群聊？

A: 使用 `create_app_chat` 方法：
```python
chat_id = client.create_app_chat(
    name="群聊名称",
    owner="owner_userid",
    userlist=["user1", "user2"]
)
```

## 更多信息

- [企业微信 API 文档](https://developer.work.weixin.qq.com/document/)
- [项目文档](../../README.md)
- [接口文档](../../interface/README.md)

